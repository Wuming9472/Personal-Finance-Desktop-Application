# Test archiviati

Questa cartella contiene i test spostati fuori dal package di build principale.
`DateUtilTest` è stato preservato qui solo come riferimento storico: non viene
eseguito dalle suite JUnit configurate nel progetto e non influenza la
copertura automatica, ora focalizzata sugli altri helper presenti in
`src/it/unicas/project/template/address/util`.
